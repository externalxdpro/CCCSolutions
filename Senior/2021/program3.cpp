#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> p(n), w(n), d(n);

    for (int i = 0; i < n; i++)
    {
        cin >> p[i] >> w[i] >> d[i];
    }

    cout << "0" << endl;

    return 0;
}